import PlaceholderPage from "@/components/PlaceholderPage";

export default function Page() {
  return (
    <PlaceholderPage
      title="Logs"
      description="Webhook events, signal fires, and bot lifecycle logs will live here. Until then, query the signals table directly."
    />
  );
}
