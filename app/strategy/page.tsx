import PlaceholderPage from "@/components/PlaceholderPage";

export default function Page() {
  return (
    <PlaceholderPage
      title="Strategy"
      description="Strategy config (Pivot Master Bot v4 parameters, confluence weights, threshold tuning) will live here."
    />
  );
}
